Все сущности JS - объекты с набором свойством имя:значение
При помощи точечной записи можно менять свойства объекта и добавлять свойства объекта

Window - глобальный объект в веббраузере
Global - глобальный объект в Node JS
GlobalThis - универсальный глобальный объект

Метод объекта - это свойство, значением которого является функция. Следовательно методы можно вызывать.

JSON - javaScript Object Notation - формат передачт данных между устройствами в интернете. Передача выполняется
при помощи строки. Для преобразования строки в JS объект используется метод JSON.parse, для обратного преобразования
используется метод JSON.stringify
