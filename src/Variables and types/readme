var -
let - значение пременных, объявленных с помощью let  можно переприсваивать
const - значение пременных, объявленных с помощью const переприсвоить нельзя. исключением является ситуация
когда значением const  является объект. В этом случае свойства внутри объекта можно переприсвоить, не изменяя
саму переменную!


Примитивные типы JS: string - строка; boolean - логическое выражение (true иили false); number - число ;
null - null( у переменной нет значения. Присваивается только по твоему желанию);
undefined - undefined (значение не определенно, автоматически присваивается переменной если значение не задано);
symbol- редкоиспользуемое, для специфических задач

Ссылочные типы: объект (переменная содержит лишь ссылку на ячейку памяти, в которой хранится объект)



